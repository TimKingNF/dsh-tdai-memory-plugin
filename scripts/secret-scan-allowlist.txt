# 密钥扫描白名单：每行一个**字面串**，只要某行里包含该串，就跳过这一行的判定。
#
# 只允许放"确定不是凭据"的东西，并且必须写清理由 —— 每次豁免都是一次有意识的决定，
# 而不是把启发式规则放宽（放宽会让真正的手滑被一起放过）。
#
# 格式：<字面串>  # 理由（# 之后的都是注释）

PROXY/src/injection/                            # MemoryProxy 的源码路径前缀，看着像高熵串而已
MemoryKnowledge/v3-api-memoryknowledge-doc.md   # TencentDB-Agent-Memory 的公开 API 文档路径
